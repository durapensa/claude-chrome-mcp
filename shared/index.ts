/**
 * Central export point for all TypeScript types in Claude Chrome MCP
 */

// Export existing types
export * from './types';

// Export MCP tool types
export * from './mcp-tool-types';

// Export protocols if they have types
// Note: protocols.ts might need to be converted to TypeScript
// export * from './protocols';